# Morrisons WSO2 Agent Manager – Python Dependencies
# Install: pip install -r requirements.txt

# Core
pydantic>=2.0.0

# Async HTTP server
aiohttp>=3.9.0

# ── OpenTelemetry (required for WSO2 Agent Manager observability) ────────────
# SDK core
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0

# Semantic conventions (attribute names WSO2 Choreo expects)
opentelemetry-semantic-conventions>=0.45b0

# OTLP exporters – WSO2 Agent Manager injects OTEL_EXPORTER_OTLP_ENDPOINT
# and this package sends traces/metrics to Choreo's collector
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0   # fallback if gRPC unavailable

# Auto-instrumentation for aiohttp (covers HTTP server spans automatically
# when WSO2 "Enable auto instrumentation" is ticked)
opentelemetry-instrumentation-aiohttp-server>=0.45b0
opentelemetry-instrumentation-aiohttp-client>=0.45b0

# W3C TraceContext propagation (required for distributed tracing across agents)
opentelemetry-propagator-b3>=1.24.0
